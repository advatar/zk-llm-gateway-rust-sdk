# GitHub Actions status

CI is temporarily disabled to avoid consuming GitHub Actions minutes.

## Re-enable CI

Rename the workflow file back to a `.yml` file:

```bash
mv .github/workflows/ci.yml.disabled .github/workflows/ci.yml
```

Then push to GitHub.
